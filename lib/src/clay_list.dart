import 'package:flutter/material.dart';

class ClayList extends StatefulWidget {
  const ClayList({Key? key}) : super(key: key);

  @override
  _ClayListState createState() => _ClayListState();
}

class _ClayListState extends State<ClayList> {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Container(),
      ),
    );
  }
}
